# Cloudflare Pages project configuration
name = "hkdse-physics-ai-tutor"
compatibility_date = "2024-01-01"
pages_build_output_dir = "frontend"

# Pages-specific config (handled via dashboard for secrets)
# Functions are auto-detected from /functions directory

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "hkdse-physics-tutor-db"
database_id = "4fac0ca6-eefc-4c5c-878c-6633e0058afd"

# Vectorize binding for RAG knowledge base
[[vectorize]]
binding = "VECTORIZE"
index_name = "hkdse-physics-kb"

# R2 bucket for document storage (optional - enable R2 in dashboard first)
# [[r2_buckets]]
# binding = "R2_BUCKET"
# bucket_name = "hkdse-physics-docs"

# Required environment variables (set in Cloudflare Dashboard):
# - QWEN_API_KEY: 通义千问 API key
# - DEEPSEEK_API_KEY: DeepSeek API key
# - OPENAI_API_KEY: OpenAI API key (for embeddings)
# - AUTH_SECRET: Secret for signing session cookies (generate with: openssl rand -hex 32)
# - RESEND_API_KEY: Resend.com API key for magic link emails
